// It instructs the compiler to reserve a memory location.
/*
We declare a variable in C like: 
    int a;
    float b;
    char c;
Here, a, b, and c are names of the memory location.
*/

/*
    Other Variations:
        int a; // declaares an integer variable a
        float b; // declares a float variable b
        int i = 10; // Delares and initializes i with 10
        int j = i; // declares j and initializes it with i
        int a = 2, b = 3, c = 4, d = 5; // declares and initializes multiple variables
        int j1 = a+j-i; // valid
        float a = 1.1; // also valid
        assigning the same value to multiple different variables is valid
            eg: int a = b = c = d = 30; is valid

        invalid: 
            a is used before declaration
            float b = a+3;
            
*/

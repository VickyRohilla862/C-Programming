
#include <stdio.h>

int main() {
    float result = 3.0 / 8 - 2;
    printf("The result is: %f\n", result);
    return 0;
}

// The output will be of float type because arithmatic operation between an integer and a float will be always float.

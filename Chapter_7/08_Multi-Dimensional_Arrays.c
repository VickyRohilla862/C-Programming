/*
An array can be 2 dimensional/ 3 dimensional/ n dimensional.
A 2D array can be defined like this:
    int arr[3][2] = {{1, 4}, 
                     {7, 9}, 
                     {11, 22}}; // here [3] represents the columns and [2] represents the rows
    // array inside another array is called 2D array.
    // 3D arrays are just arrays inside arrays inside arrays and so on.

We can access the element of this array as arr[0][0], arr[0][1], arr[1, 0], arr[1, 2] and so on...
*/
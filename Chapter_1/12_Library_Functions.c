

// C language has a lot of valuable library functions which is used to carry out certain tasks.
// For instance, printf() function is used to print values on screen.

//example:

#include <stdio.h>
int main(){
    int i = 10;
    printf("This id %d\n", i);
    // %d for integers
    // %f fro floating point numbers
    // %c for characters
    return 0;
}

//output:
// This is 10